{
  "srvcId": "${artifactId}-${version}",
  "nodeId": "localhost",
  "descriptor": {
    "exec": "java -Dport=%p -DlogLevel=INFO -jar ../mod-inventory-match/target/mod-inventory-match-fat.jar embed_postgres=true"
  }
}

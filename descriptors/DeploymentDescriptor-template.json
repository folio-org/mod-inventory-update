{
  "srvcId": "${artifactId}-${version}",
  "nodeId": "localhost",
  "descriptor": {
    "exec": "java -Dport=%p -DlogLevel=INFO -jar ../mod-inventory-match/target/mod-inventory-update-fat.jar embed_postgres=true"
  }
}

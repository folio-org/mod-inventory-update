{
  "description": "Prevent deletion of existing record (even though missing in input) using pattern matching.",
  "properties": {
    "ifField": {
      "description": "Name of field to match",
      "type": "string"
    },
    "matchesPattern": {
      "description": "Regex pattern to match field by",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "ifField", "matchesPattern"
  ]
}

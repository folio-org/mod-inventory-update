{
  "description": "Defines deletion block for existing record missing in input, using pattern matching.",
  "properties": {
    "field": {
      "description": "Name of field to match",
      "type": "string"
    },
    "pattern": {
      "description": "Block deletion of a record if 'field' is matched by 'pattern'.",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "field", "pattern"
  ]
}
